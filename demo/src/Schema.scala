/*
object schema {
    import DB.ctx._
    val cities = quote {
        querySchema[City]("cities")
    }
    val countries = quote {
        querySchema[Country]("countries")
    }
    val continents = quote {
        querySchema[Continent]("continents")
    }
}
*/